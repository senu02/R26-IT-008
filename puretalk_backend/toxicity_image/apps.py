from django.apps import AppConfig


class ToxicityImageConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'toxicity_image'
    verbose_name = 'Toxicity Image Detection'
